<h4>{{ 'Hello! You can add here your review.'|trans }}</h4>
{{ form }}
<h4>{{ 'You can see list of all reviews here.'|trans }}</h4>
{% for item in items %}
  <div class="row underline_gbook">
    <div class="col-md-6">
      <a href="{{ item.avatar }}" target="_blank"><img src="{{ item.avatar }}" alt="user profile image"></a>
      <p><span>{{ 'Name:'|trans }} </span>{{ item.name }}</p>
      <p><span>{{ 'Email:'|trans }} </span>{{ item.mail }}</p>
      <p><span>{{ 'Phone:'|trans }} </span>{{ item.phone }}</p>
      <p><span>{{ 'Review:'|trans }} </span>{{ item.comment }}</p>
      <p><span>{{ 'Submitted:'|trans }} </span>{{ item.created }}</p>
      {% if user.hasPermission('administer nodes') %}
        <a href="/anzy/gbookDel/{{ item.id }}" class="deleteGbook use-ajax" data-dialog-type="modal">{{ 'Delete'|trans }}</a>
        <a href="/admin/anzy/gbookChange/{{ item.id }}" class="deleteGbook">{{ 'Edit'|trans }}</a>
      {% endif %}
    </div>
    <div class="col-md-6">
      <div class="image-container"><a href="{{ item.image }}" target="_blank"><img src="{{ item.image }}" alt="user image"></a></div>
    </div>
  </div>
{% endfor %}
